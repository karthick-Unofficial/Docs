// import React from "react";
// import SearchResults from "./SearchResults";

// import { shallow } from "enzyme";

// jest.mock("mapbox-gl/dist/mapbox-gl", () => ({
// 	Map: () => ({})
// }));

/**
 * FIXME: Figure out Enzyme error and readd test
 * See entityCollectionToggle.test.js for more information
 */
describe("SearchResults", () => {
	it("renders correctly with text", () => {
		// const wrapper = shallow(<SearchResults label="Results" searchTerms="" />);
		// expect(wrapper).toMatchSnapshot();
		return new Promise(resolve => {
			resolve();
		});
	});

	it("renders correctly without text", () => {
		// const wrapper = shallow(<SearchResults plabel="Results" searchTerms="" />);
		// expect(wrapper).toMatchSnapshot();
		return new Promise(resolve => {
			resolve();
		});
	});
});
